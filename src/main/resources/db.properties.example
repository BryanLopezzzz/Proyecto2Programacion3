   # =============================================
   # CONFIGURACIÓN DE BASE DE DATOS - HOSPITAL
   # =============================================
   # INSTRUCCIONES:
   # 1. Copia este archivo y renómbralo a: db.properties
   # 2. Cambia db.password por TU contraseña de MySQL
   # 3. NO hagas commit del archivo db.properties

db.url=jdbc:mysql://localhost:3306/hospital?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
db.user=root
db.password=CAMBIA_ESTO_POR_TU_PASSWORD
db.pool.size=10